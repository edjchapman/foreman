# AGENTS.md

Agent context for this repo lives in **[CLAUDE.md](CLAUDE.md)** — stack, commands, layout,
conventions, and the pre-PR gate (`make preflight`).

This file is a pointer so non-Claude agents find the context too; keep the content in
`CLAUDE.md` (single source of truth) to avoid drift.
