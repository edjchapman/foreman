#!/bin/sh
# Commit-msg hook: validate the commit subject against the commit standard.
# Activate with: git config core.hooksPath .githooks
# Blocking: rejects a non-conforming subject (mirrors the required commit-style CI check).

exec "$(git rev-parse --show-toplevel)/scripts/check-commit-msg.sh" "$1" --strict
