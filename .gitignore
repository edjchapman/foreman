# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Django
*.sqlite3
*.log
staticfiles/
media/

# Celery (beat writes these at runtime)
celerybeat-schedule*
celerybeat.pid

# Env / secrets
.env

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code — ignore only the personal, machine-specific bits.
# .claude/settings.json and .claude/hooks/ are committed (shared): Claude Code on
# the web clones the repo with no global config, so it needs them present.
.claude/agents
.claude/commands
.claude/skills
.claude/rules
.claude/settings.local.json
.mcp.json
